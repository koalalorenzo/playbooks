all:
  vars:
    ansible_user: koalalorenzo
    tailscale_accept_dns: "true"
  children:
    web:
      children:
        nas:
    homeautomation:
      hosts:
        zeroberry.local:
    pihole:
      vars:
        tailscale_accept_dns: "false"
      hosts:
        zeroberry.local:
    nas:
      hosts:
        nasberry:
