all:
  vars:
    ansible_user: koalalorenzo
    tailscale_accept_dns: "true"
  children:
    web:
      children:
        nas:
    homeautomation:
      hosts:
        zeroberry:
    dns:
      vars:
        tailscale_accept_dns: "false"
      hosts:
        zeroberry:
    nas:
      hosts:
        nasberry:
